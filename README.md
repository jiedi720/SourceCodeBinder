# SourceUnite

**SourceUnite** 是一款轻量级、功能强大的源码整合工具。它可以快速扫描您的项目文件夹，智能过滤非文本文件（乱码），并将所有源代码合并为一个结构清晰的 **Markdown** 文档，进而导出为精美的 **PDF** 文档。

非常适合开发者进行代码备份、技术审计、或是将整个项目提交给 AI 进行深度分析。

---

## ✨ 核心功能

* **🔍 智能扫描与过滤**：
* 自动识别多种编程语言（Python, JS, C++, Java, HTML/CSS, SQL, YAML 等）。
* **乱码防御系统**：通过二进制特征分析，自动跳过 `.pyc`、图片、库文件等乱码来源。
* 支持自定义排除目录（如 `.git`, `node_modules`, `venv` 等）。


* **📄 结构化导出**：
* 生成带目录（Table of Contents）的 Markdown，支持页内锚点跳转。
* 注入 GitHub 风格的 CSS 样式，生成的 PDF 具有极高的阅读舒适度。


* **可视化操作**：
* 基于 `CustomTkinter` 的现代暗黑风格界面。
* **实时进度条**：在大规模项目合并时提供精确的百分比进度。
* **一键式体验**：集成浏览、打开目录、合并、转换全流程。



---

## 🛠️ 安装要求

在运行本项目之前，请确保您的系统中已安装以下环境：

1. **Python 3.8+**
2. **wkhtmltopdf** (PDF 转换核心引擎)
* [下载地址](https://wkhtmltopdf.org/downloads.html)
* 安装后请确保路径与 `function/pdf.py` 中的配置一致。



---

## 🚀 快速开始

### 1. 克隆/下载项目

确保您的目录结构如下：

```text
SourceUnite/
├── SourceUnite.py          # 程序入口
├── function/
│   ├── combine.py          # 扫描与合并逻辑
│   └── pdf.py              # PDF 转换逻辑
├── gui/
│   └── main_gui.py         # GUI 界面代码
└── README.md

```

### 2. 安装依赖库

在项目根目录运行：

```bash
pip install customtkinter markdown2 pdfkit

```

### 3. 运行程序

```bash
python SourceCodeBinder.py

```

---

## 📖 使用指南

1. **选择路径**：点击“浏览”选择您的项目根目录，或直接粘贴路径。
2. **打开确认**：点击输入框右侧的“打开”按钮，可快速弹出系统资源管理器确认文件夹内容。
3. **合并代码**：点击 **① 合并为 Markdown**。进度条将显示扫描和写入的实时进度。
* *提示：生成的 .md 文件将直接保存在您的源项目目录下。*


4. **导出 PDF**：合并完成后，点击 **② 导出为 PDF**。程序将自动完成渲染。

---

## 🛡️ 技术细节说明

### 乱码过滤逻辑

为了防止合并后的文档出现大量乱码，本项目采用了双重校验机制：

1. **后缀过滤**：仅处理白名单内的代码后缀。
2. **内容嗅探**：即使后缀符合，若在文件前 1024 字节检测到 `\0` (Null Byte) 或 UTF-8 解码失败，该文件将被标记为二进制文件并自动跳过。

### 样式注入

导出的 PDF 包含预设的 CSS，优化了代码块的显示：

* **字体**：优先使用 `Consolas` 和 `Segoe UI`。
* **间距**：合理的行高与内边距，确保长代码不拥挤。

---

## 🤝 贡献

欢迎提交 Issue 或 Pull Request 来改进扫描算法或增加更多语言的支持。

---

### 💡 代码块作用解释

> **README 说明：**
> * **项目结构**: 清晰地展示了 `SourceCodeBinder.py` 作为入口如何调用 `function/` 里的逻辑。
> * **进度条亮点**: 强调了在处理大型项目（如 OpenFOAM 案例或大型 Web 项目）时的用户体验。
> * **安装提示**: 特别标注了 `wkhtmltopdf` 的依赖，这是用户最容易遇到问题的地方。
> 
> 